//  =================  45 QUESTIONS ASSINGMENT   ===================
// ==========   Q31   ========= 
let users = [];
// check if the list of users is not empty
if (users.length === 0) {
    console.log("We  Need to Find Some Users! ");
}
else {
    // loop through the array and print each  users
    for (let user of users) {
        console.log(user);
    }
}
// remove all usernames from the array 
users = [];
// check again if the list of the user is not empty
if (users.length === 0) {
    console.log("We  Need to Find Some Users! ");
}
